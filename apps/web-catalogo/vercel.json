{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd ../.. && npm ci",
  "buildCommand": "npm run build",
  "crons": [
    {
      "path": "/api/cron/expire-citas",
      "schedule": "*/10 * * * *"
    }
  ]
}
