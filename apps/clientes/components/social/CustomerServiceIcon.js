import { Headset } from 'lucide-react-native';

/** Atención al cliente (misma escala que logos de contacto). */
export function CustomerServiceIcon({ size = 28, color = '#C5A368' }) {
  return <Headset size={size} color={color} strokeWidth={2} />;
}
